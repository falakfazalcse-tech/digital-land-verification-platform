# LandDeal Backend API - Payment Module

Production-grade Express.js REST API with MySQL database for real estate payments (`payment1.html` to `payment5.html`).

---

## 🚀 Setup & Installation

### 1. Prerequisites
- Node.js (v18+)
- MySQL Server (v8+)

### 2. Database Setup
Log into MySQL and execute the schema initialization script:
```bash
mysql -u root -p < database/schema.sql